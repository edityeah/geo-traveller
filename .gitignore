node_modules
dist
.astro
.env
.env.local

# Notion-sourced posts (rebuilt every deploy)
src/content/posts/notion/
# Images now live in Cloudflare R2 (images.geo-traveller.com). Builds upload
# there; this folder only ever appears as a local-dev fallback when R2 env
# vars are absent. Never commit it — it bloats the repo.
public/img/generated/

# OS
.DS_Store

# Migration artifacts
migration-report.md
scripts/migrate-wp/output/
